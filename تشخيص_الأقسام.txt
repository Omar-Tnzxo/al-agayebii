═══════════════════════════════════════════════════════════════
           تشخيص مشكلة عدم ظهور المنتجات في صفحة التصنيف
═══════════════════════════════════════════════════════════════

📋 المشكلة:
   التصنيف يحتوي على منتجات (مثلاً 2 منتج)
   لكن عند الدخول على صفحة التصنيف، لا تظهر المنتجات

───────────────────────────────────────────────────────────────
🔍 التشخيص:
───────────────────────────────────────────────────────────────

تم إضافة logging شامل في:

1️⃣  صفحة التصنيف (app/category/[type]/page.tsx):
    🔍 جلب بيانات التصنيف
    📦 التصنيف المطابق
    🔍 جلب المنتجات من URL
    📊 بيانات المنتجات المستلمة
    ✅ المنتجات بعد الفلترة

2️⃣  API المنتجات (app/api/products/route.ts):
    🔍 API Products - Parameters
    🔍 فلترة حسب category_type
    📦 من Cache (إذا موجود)
    ✅ عدد المنتجات المسترجعة
    ❌ خطأ في جلب المنتجات (إن وجد)

───────────────────────────────────────────────────────────────
🧪 خطوات الاختبار:
───────────────────────────────────────────────────────────────

1. شغّل development server:
   npm run dev

2. افتح Terminal وراقب الـ logs

3. اذهب إلى صفحة تصنيف (مثلاً):
   http://localhost:3000/category/electrical

4. راقب Terminal، يجب أن ترى:
   ─────────────────────────────────────
   🔍 جلب بيانات التصنيف: electrical
   📦 التصنيف المطابق: { id: ..., name: "...", type: "electrical" }
   🔍 جلب المنتجات من: http://localhost:3000/api/products?category=electrical
   🔍 API Products - Parameters: { category: 'electrical', ... }
   🔍 فلترة حسب category_type: electrical
   ✅ عدد المنتجات المسترجعة: X
   📊 بيانات المنتجات المستلمة: { ... }
   ✅ المنتجات بعد الفلترة: X منتج
   ─────────────────────────────────────

───────────────────────────────────────────────────────────────
🔎 السيناريوهات المحتملة:
───────────────────────────────────────────────────────────────

السيناريو 1: التصنيف غير موجود
   الأعراض:
   - ❌ لم يتم العثور على التصنيف
   - الصفحة تعرض 404

   الحل:
   - تأكد من أن التصنيف موجود في قاعدة البيانات
   - استخدم check-category-products.sql للتحقق

السيناريو 2: المنتجات موجودة لكن category_type خاطئ
   الأعراض:
   - ✅ عدد المنتجات المسترجعة: 0
   - لكن في قاعدة البيانات المنتجات موجودة

   الحل:
   - تحقق من أن category_type في جدول products يطابق type في categories
   - استخدم الاستعلام رقم 3 في check-category-products.sql

السيناريو 3: المنتجات غير نشطة (is_active = false)
   الأعراض:
   - ✅ عدد المنتجات المسترجعة: 0
   - المنتجات موجودة لكن is_active = false

   الحل:
   - فعّل المنتجات من Dashboard
   - أو عدّل الاستعلام ليشمل المنتجات غير النشطة

السيناريو 4: مشكلة في Cache
   الأعراض:
   - 📦 من Cache: 0 منتج
   - حتى بعد إضافة منتجات جديدة

   الحل:
   - امسح Cache أو انتظر 15 دقيقة
   - أو أعد تشغيل server

السيناريو 5: مشكلة في بنية البيانات المرجعة
   الأعراض:
   - ✅ عدد المنتجات المسترجعة: X
   - لكن المنتجات بعد الفلترة: 0

   الحل:
   - تحقق من بنية البيانات في 📊 بيانات المنتجات المستلمة
   - قد تكون data متداخلة (data.data.data)

───────────────────────────────────────────────────────────────
🗄️ فحص قاعدة البيانات:
───────────────────────────────────────────────────────────────

استخدم الملف: check-category-products.sql

الاستعلامات المهمة:

1. عرض جميع التصنيفات مع عدد المنتجات
2. عرض المنتجات حسب التصنيف
3. التحقق من المنتجات التي ليس لها تصنيف صحيح
4. التصنيفات التي ليس لها منتجات
5. التصنيفات التي لها منتجات
6. فحص تصنيف معين

───────────────────────────────────────────────────────────────
✅ الحلول المحتملة:
───────────────────────────────────────────────────────────────

1. تصحيح category_type في المنتجات:
   UPDATE products 
   SET category_type = 'electrical'
   WHERE category_type IS NULL OR category_type = '';

2. تفعيل المنتجات:
   UPDATE products 
   SET is_active = true
   WHERE category_type = 'electrical';

3. ربط المنتجات بالتصنيف الصحيح:
   UPDATE products p
   SET category_type = c.type
   FROM categories c
   WHERE p.category_id = c.id
   AND (p.category_type IS NULL OR p.category_type = '');

───────────────────────────────────────────────────────────────
📝 ملاحظات:
───────────────────────────────────────────────────────────────

• تم تعديل URL في صفحة التصنيف من:
  /api/products?category_type=... 
  إلى:
  /api/products?category=...
  
  لأن API يبحث عن params.category وليس params.category_type

• تم إصلاح بنية البيانات المرجعة:
  productsData.data?.data بدلاً من productsData.data

• Logs سيساعدك في تحديد المشكلة بدقة

═══════════════════════════════════════════════════════════════
                      🎯 ابدأ التشخيص الآن
═══════════════════════════════════════════════════════════════
